def simpleinterest():
    p = int(input("enter the principle value:"))
    t = int(input("enter time period:"))
    r = int(input("enter rate of interest:"))
    simpleinterest = int((p * t * r) / 100)
    print("simple interest for the given timeperiod rate and principal is:",simpleinterest)
simpleinterest()
